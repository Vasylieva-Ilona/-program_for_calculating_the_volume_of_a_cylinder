alert("Enter the data to create a formula for calculating the volume of a cylinder");

alert(radius = " " + prompt("What radius do you want to choose for the formula?"));

alert(height = " " + prompt("What height should you choose for the formula?"));

var test = 3.14159 * (radius * radius) * height;

alert("You chose the base area value equal to " + test);